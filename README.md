# structured-thinking-agent
Structured reasoning agent that turns messy situations into node-edge graphs using two thinking modes: ISTJ (cause → risk → action) and MECE (clean, non-overlapping problem map).
